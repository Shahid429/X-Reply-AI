name: Build and Release X Reply AI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: {}
  release:
    types: [created]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Allow pushing to repository
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          # For releases, checkout the main branch instead of detached HEAD
          ref: ${{ github.event_name == 'release' && 'main' || github.ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint code
        run: npm run lint

      - name: Build extension
        run: npm run prod

      - name: Verify build
        run: |
          ls -la dist/
          if [ ! -f "dist/manifest.json" ]; then
            echo "Build failed: manifest.json not found"
            exit 1
          fi
          if [ ! -f "dist/background.js" ]; then
            echo "Build failed: background.js not found"
            exit 1
          fi

      - name: Create release archive
        run: |
          mkdir -p release
          cd dist
          # Use different naming based on event type
          if [ "${{ github.event_name }}" = "release" ]; then
            VERSION="${{ github.event.release.tag_name }}"
          else
            # Use date and short commit hash for development builds
            VERSION="$(date +%Y%m%d)-${{ github.sha }}"
            VERSION="${VERSION:0:15}"  # Truncate to reasonable length
          fi
          EXTENSION_NAME="X-Reply-AI-Extension-v${VERSION}.zip"
          zip -r "../release/${EXTENSION_NAME}" .
          cd ..
          echo "Created extension: ${EXTENSION_NAME}"
          ls -la release/

      - name: Upload extension archive
        uses: actions/upload-artifact@v4
        with:
          name: x-reply-ai-extension
          path: release/
          retention-days: 30

      - name: Commit release to repo folder
        if: github.event_name != 'pull_request'
        run: |
          # Create releases folder if it doesn't exist
          mkdir -p releases
          
          # Clean up old releases (keep only the latest)
          cd releases
          rm -f *.zip
          cd ..
          
          # Copy the new archive to releases folder
          cp release/*.zip releases/
          
          # Configure git
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # Add and commit the release file
          git add releases/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            NEW_FILE=$(ls releases/*.zip | head -1 | xargs basename)
            git commit -m "🤖 Add extension release: ${NEW_FILE}" || echo "Commit failed, possibly due to conflicts"
            git push origin main || echo "Push failed, but build artifacts are still available"
          fi

      - name: Upload release archive (tagged)
        if: github.event_name == 'release'
        uses: actions/upload-artifact@v4
        with:
          name: x-reply-ai-release-${{ github.event.release.tag_name }}
          path: release/
          retention-days: 90

      - name: Upload release assets
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v1
        with:
          files: release/X-Reply-AI-Extension-v${{ github.event.release.tag_name }}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}